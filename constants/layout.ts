/**
 * Constantes de mise en page pour une cohérence visuelle dans l'app.
 */
export const Layout = {
  /** Espacement entre sections / cartes */
  sectionGap: 24,
  /** Padding des cartes */
  cardPadding: 16,
  /** Padding horizontal des écrans */
  screenPaddingHorizontal: 24,
  /** Padding top des écrans (sous status bar) */
  screenPaddingTop: 80,
} as const;
